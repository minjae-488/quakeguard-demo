#  PRD v2 — QuakeGuard (퀘이크가드)
### 제4회 재난안전데이터 활용 창업경진대회 | 제품·서비스 개발 부문
> **v2 개정 이유: 마감 7/7(화) 자정, 실질 개발 기간 5일 기준으로 전면 현실화**

---

##  스코프 결정 원칙

| 항목 | v1 (기존) | v2 (현실화) |
|------|-----------|-------------|
| 개발 기간 | 8주 | **5일** |
| 목표 | 완성된 B2G SaaS | **핵심 흐름 1개가 실제 동작하는 데모** |
| 데이터 소스 | 8개 실시간 연동 | **1개 (safetydata CSV 사전 다운로드)** |
| AI 구성 | LangGraph Agent + ML 예측모델 | **LLM API 직접 호출 (1개 함수)** |
| 프론트엔드 | React + React Native | **React 웹 단일 페이지** |
| 백엔드 | FastAPI + PostgreSQL + Redis | **없음 (브라우저에서 직접 LLM 호출)** |
| 인프라 | AWS + Docker + CI/CD | **Vercel 무료 배포** |
| 모바일 앱 | O | **X (제외)** |
| WebSocket | O | **X (제외)** |

---

## 1. 제품 한 줄 정의

> **"지진 발생 상황을 입력하면, 공개 재난안전데이터를 근거로 AI가 담당 공무원의 즉각 조치사항을 자동 생성해주는 웹 도구"**

---

## 2. 문제 정의 (Why)

- 지진은 예측 불가 + **초기 수 분이 인명피해를 결정**하는 재난
- 현 공무원 대응: 수동 데이터 조회 → 수기 보고서 작성 → 전화 지시 → FAX
- **지자체별 역량 편차 심각**: 전담 인력 없는 기초자치단체 다수 (비공식 추정)

### 핵심 Pain Point
| 문제 | 현황 | 영향 |
|------|------|------|
| 위험 시설 우선순위 없음 | 전수 확인 시도 | 인력 분산, 누락 위험 |
| 보고서 작성에 시간 소요 | 직접 수기 작성 | 행정 부담, 오기재 위험 |
| 과거 데이터 활용 미흡 | 경험치 의존 | 동일 피해 반복 |

---

## 3. 솔루션 개요 (What)

### 제품명
**QuakeGuard (퀘이크가드)**
- 부제: 지진 대응 AI 조치 도우미
- 형태: **단일 페이지 웹 앱 (SPA)**

### 핵심 흐름 (데모에서 실제 동작할 것)
```
① 사용자(공무원)가 지진 정보 입력
   - 발생 위치 (시/군/구 선택)
   - 규모 (매그니튜드)
   - 발생 시각

② 사전 로드된 safetydata 데이터에서 해당 지역 위험 정보 조회
   - 내진 취약 건축물 목록
   - 급경사지 위험지구 목록
   (→ 실제 CSV 데이터 또는 가상 데이터 사용, 공고문에서 허용 명시)

③ LLM API(Claude 또는 GPT-4o)에 데이터 + 상황 컨텍스트 전달

④ AI가 조치사항 자동 생성 출력
   - 즉시 조치 TOP 3 (0~30분)
   - 1시간 내 조치 TOP 3
   - 자동 상황보고서 초안 (텍스트)
```

---

## 4. 활용 데이터 (현실화)

### 실제 확보할 데이터 (1종)
| 데이터 | 출처 | 확보 방법 | 활용 방식 |
|--------|------|-----------|-----------|
| 내진 취약 건축물 현황 | safetydata.go.kr | **CSV 사전 다운로드** (회원가입 후 즉시 가능) | 지역 선택 시 필터링 |

> **주의**: 기상청 지진 조기경보 API는 실시간 연동 시 별도 승인 필요 가능성이 있어 **이번 데모에서는 제외**. 지진 정보는 사용자가 직접 입력.

### 가상 데이터 보완 (공고문 허용)
- 공고문 명시: *"실데이터가 부족한 경우, 가상데이터로 보완"* 허용
- 2016 경주 지진(M 5.8), 2017 포항 지진(M 5.4) 시나리오 데이터를 가상으로 구성
- 프롬프트 가이드 자료의 예시 데이터 형식 참고하여 생성

### 향후 확장 계획 (계획서에만 서술)
- 기상청 지진 조기경보 API 실시간 연동
- 급경사지·옹벽 위험지구 정보 (safetydata)
- 국가재난관리정보시스템 대피소 현황
- 도로·교량 점검 현황 (국토부)

---

## 5. AI 구성 (현실화)

### 이번 데모에서 실제 구현할 것

```
[브라우저]
  사용자 입력 (지역, 규모, 시각)
       ↓
  지역 매칭 → 내진취약 건물 목록 필터링 (JSON/CSV)
       ↓
  LLM API 호출 (Claude API 또는 OpenAI API)
  4. **AI 상황보고서 생성 (핵심 기능)**
   - 선택된 파라미터(지역, 지진 규모 등), 필터링된 공공데이터(취약 건물, 대피소), 및 프롬프트를 AI 모델(LLM)에 전달.
   - **[대회 주최측 프롬프트 가이드라인 준수]** AI는 반드시 아래 6가지 포맷으로 결과를 출력해야 함:
     1. 위험단계 판단
     2. 대피 가능 위험 TOP 3
     3. 즉시 대피 유도
     4. 접근 차단 위치
     5. 우선 확인 CCTV/계측기
     6. 즉시 연락 기관
   - AI가 반환한 결과를 실시간 스트리밍(또는 로딩 후 표시) 방식으로 화면 우측 패널에 렌더링.
```

### 구현하지 않는 것 (이유)
| 기능 | 제외 이유 |
|------|-----------|
| LangGraph AI Agent | 5일 내 구현 불가, 과도한 복잡도 |
| ML 피해 예측 모델 | 학습 데이터 확보·전처리만 수일 소요 |
| MCP 다중 API 연동 | API 승인 절차 시간 소요 |
| 실시간 WebSocket | 백엔드 서버 없이 진행하므로 불필요 |

---

## 6. 핵심 기능 명세 (데모 범위)

### F1. 지진 상황 입력 패널
- 지역 선택 (시/도 → 시/군/구 드롭다운)
- 규모 입력 (슬라이더, 1.0~9.0)
- 발생 시각 (datetime picker)
- [AI 분석 시작] 버튼

### F2. 위험 시설물 표시
- 선택 지역의 내진 취약 건축물 목록 카드 표시
- 건물명, 준공연도, 내진설계 여부, 위험도 등급

### F3. AI 조치사항 생성  핵심 기능
- LLM이 실시간으로 텍스트 생성 (스트리밍 출력)
- 즉시 조치 / 1시간 내 조치 / 주의사항 섹션으로 구분
- 복사 버튼 (클립보드 복사)

### F4. 상황보고서 초안 출력
- LLM이 행정 보고서 형식으로 텍스트 출력
- 브라우저 인쇄(Ctrl+P) / 텍스트 복사 기능

### F5. 시나리오 빠른 선택 (데모용)
- "2016 경주 지진 (M5.8)" 버튼 클릭 → 자동 입력 + 분석
- "2017 포항 지진 (M5.4)" 버튼 클릭 → 자동 입력 + 분석
- 심사위원 앞 데모 시 빠른 시연용

---

## 7. 기술 스택 (5일 현실 버전)

```
 프론트엔드 (전부)
  - React 18 + TypeScript (Vite)
  - Vanilla CSS (커스텀 디자인)
  - Leaflet.js (지도 - 무료, 인증 불필요)
  - Claude API / OpenAI API (브라우저에서 직접 호출)
  - 데이터: JSON 파일 로컬 번들 (CSV → JSON 변환)

 배포
  - Vercel (GitHub 연결, 무료, 5분 배포)

 필요한 외부 계정
  - Anthropic API key (또는 OpenAI)
  - Vercel 계정
  - safetydata.go.kr 회원가입 (데이터 다운로드용)
```

> **왜 백엔드 없이?** 5일 안에 UI + AI 연동 품질을 높이는 것이 우선. API 키는 환경변수로 프론트에서 관리(데모 목적).

---

## 8. 개발 일정 (5일)

| 날짜 | 할 일 | 완료 기준 |
|------|--------|-----------|
| **7/2 (수) 오늘** | safetydata 데이터 다운로드·확인, Vite 프로젝트 생성, 레이아웃 초안 | 폴더 구조 + 빈 컴포넌트 셋업 |
| **7/3 (목)** | UI 완성 (입력 패널, 지도, 위험 시설 카드) | 정적 UI 화면 완성 |
| **7/4 (금)** | LLM API 연동, 조치사항 생성 기능 | 실제 AI 응답 출력 확인 |
| **7/5 (토)** | 보고서 출력, 시나리오 버튼, 디자인 polish | 데모 시나리오 2개 완전 동작 |
| **7/6 (일)** | 버그 수정, Vercel 배포, 계획서 작성 | 배포 URL 접속 확인 |
| **7/7 (월)** | 최종 점검, 제출 서류 완성, 제출 | 접수 완료 |

> **버퍼 없음**: 각 날의 목표를 반드시 달성해야 함. 기능 추가 엄금.

---

## 9. 심사 대응 전략 (공고문 기준)

>  **주의**: 제4회 재난안전 공고문에는 구체적인 배점표가 명시되지 않음.
> 범정부 통합공고(14회)의 배점을 참고하되, 계획서에 직접 기재하지 않음.
> 아래는 공모계획서 서술 전략임.

### 공모계획서 작성 항목 (붙임2 기준)
| 항목 | 서술 전략 |
|------|-----------|
| **공익성·수익성** | 골든타임 단축 → 인명피해 감소, 소외 지자체 대응 역량 강화 |
| **데이터 활용 계획** | safetydata CSV 실사용 + 가상데이터 병행 (공고 허용), 향후 8개 소스 확장 계획 서술 |
| **기술적 성과 및 이후 계획** | 현 MVP → B2G SaaS 전환 로드맵, 지자체 구독 수익 모델 |
| **사업화 가능성** | TAM(전국 243개 기초자치단체), 행안부 스마트안전 연계 |

---

## 10. 리스크 & 대응 (5일 버전)

| 리스크 | 대응 |
|--------|------|
| safetydata 데이터 형식이 예상과 다름 | 가상 JSON 데이터로 즉시 대체 (공고 허용) |
| LLM API 비용 과다 | 데모용 최대 토큰 제한 설정, 캐싱 |
| Leaflet 지도 퍼포먼스 | 데이터 포인트 50개 이하로 제한 |
| 개발 지연 | F4(보고서) → F2(지도)를 선택적으로 제거, F1+F3만으로도 데모 가능 |

---

## 11. 제출 구성

| 제출물 | 내용 |
|--------|------|
| 공모계획서 (10페이지 이내, HWPX) | 본 PRD 기반 |
| 참가신청서 | 붙임1 양식 |
| 참가동의서 | 붙임3 양식 |
| 개인정보 수집·이용 동의서 | 붙임4 양식 |
| **시제품** | Vercel 배포 URL + 소스코드 ZIP |

---

*작성일: 2026-07-02 | v2 개정: 5일 현실화 | 공모전: 제4회 재난안전데이터 활용 창업경진대회*
