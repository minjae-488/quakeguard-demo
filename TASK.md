#  TASK.md — QuakeGuard 개발 업무 목록 v2
> 마감: 2026년 7월 7일(화) 자정
> 오늘: 2026년 7월 2일(수) | 실질 개발 기간: **5일**
> 상태:  미시작 /  진행중 /  완료 /  블로커

---

##  vs  역할 분담

| 기호 | 의미 |
|------|------|
|  | **사용자가 직접 해야 할 일** (코딩 없음) |
|  | **AI(Antigravity)가 코딩할 일** |
|  | **둘 다 확인 필요한 결정 사항** |

---

##  Day 1 — 7/2 (수) 오늘 : 환경 세팅

### 1-A. 사용자 준비 작업

- [x]  **Node.js 설치**
  - https://nodejs.org → LTS 버전 다운로드 → 설치(Next만 클릭)
  - 완료 확인: 윈도우 검색 → PowerShell 실행 → `node -v` 입력 → `v22.x.x` 같은 숫자 보이면 성공
  -  이게 안 되면 이후 모든 개발 불가

- [x] **safetydata.go.kr 회원가입 및 데이터 탐색**
  - https://www.safetydata.go.kr 접속 → 회원가입
  - 검색창에 "내진 취약 건축물" 검색 → 데이터 목록 확인
  - 검색창에 "지진 이력" 검색 → 데이터 목록 확인
  -  데이터가 다운로드 가능한지(CSV/Excel) 직접 확인 후 AI에게 알려주기
  - 다운로드 가능하면: CSV 다운로드
  - 다운로드 불가하면: AI가 가상 데이터로 대체 생성
  - **(진행완료: AI가 포항/경주 실기반 가상(Mock) 데이터를 직접 생성하여 적용 완료)**

- [x]  **LLM API 키 발급** (아래 중 1개 선택)
  - 선택A — Claude: https://console.anthropic.com → 회원가입 → API Keys → Create Key
  - 선택B — OpenAI: https://platform.openai.com → 회원가입 → API Keys → Create
  - 발급한 키(sk-ant-... 또는 sk-...) 메모장에 임시 저장
  -  유료 결제 수단 등록 필요할 수 있음 (소액: 약 $5~10 예상)
  -  어떤 것 선택했는지 AI에게 알려주기
  - **(완료: Claude API Key를 .env에 설정 완료)**

### 1-B. AI 코딩 작업 (Node.js 설치 완료 후 시작)

- [x]  **Vite + React + TypeScript 프로젝트 생성**
  - 결과물: `quakeguard/` 폴더 생성
  - 명령어 실행 및 확인

- [x]  **불필요한 기본 파일 정리**
  - `App.tsx`, `App.css`, `index.css` 초기화
  - `public/` 폴더 정리

- [x]  **폴더 구조 설계 및 빈 파일 생성**
  ```
  src/
  ├── components/
  │   ├── Header.tsx          ← 상단 헤더
  │   ├── InputPanel.tsx      ← 지진 정보 입력 폼
  │   ├── BuildingList.tsx    ← 취약 건물 목록 카드
  │   ├── MapView.tsx         ← 지도
  │   ├── AiResult.tsx        ← AI 조치사항 출력
  │   └── ReportPanel.tsx     ← 상황보고서
  ├── data/
  │   └── buildings.json      ← 건물 데이터 (CSV → JSON 변환본)
  ├── hooks/
  │   └── useLLM.ts           ← LLM API 호출 로직
  ├── types/
  │   └── index.ts            ← TypeScript 타입 정의
  └── App.tsx                 ← 메인 조립
  ```

- [x]  **디자인 시스템 설정** (`index.css`)
  - 컬러 팔레트 (재난 테마: 딥네이비, 경고 오렌지, 위험 레드)
  - Google Fonts 연결 (Inter)
  - CSS 변수 정의 (--color-bg, --color-accent 등)
  - 기본 리셋 및 공통 클래스

- [x]  **전체 레이아웃 구성** (`App.tsx`)
  - 2단 레이아웃: 좌측 입력 패널 | 우측 결과 패널
  - Header 컴포넌트 배치
  - 반응형 기본 틀

- [x]  **개발 서버 실행 확인**
  - `npm run dev` 실행 → 브라우저에서 http://localhost:5173 접속 확인
  - 사용자에게 스크린샷 요청

---

##  Day 2 — 7/3 (목) : 데이터 + 핵심 UI

### 2-A. 데이터 처리

- [x]  **다운로드한 CSV 파일을 지정 경로에 복사**
  - AI가 안내하는 경로: `quakeguard/src/data/raw/` 에 붙여넣기

- [x]  **CSV 파일 구조 분석 및 파싱 스크립트 작성**
  - 어떤 컬럼이 있는지 파악
  - 필요한 필드: 건물명, 주소(시군구), 준공연도, 내진설계여부, 용도

- [x]  **CSV → JSON 변환** (`buildings.json` 생성)
  - 지역(시군구)별로 그룹화
  - 샘플 데이터 10건 이상 확인

- [x]  **safetydata 데이터 없는 경우 가상 데이터 생성**
  - 경주시, 포항시, 울산시 3개 지역 기준
  - 건물 10~20개씩 현실적인 가상 데이터 JSON 생성

- [x]  **TypeScript 타입 정의** (`types/index.ts`)
  ```typescript
  Building, EarthquakeInput, AiResponse, Region 타입 정의
  ```

### 2-B. UI 컴포넌트 구현

- [x]  **Header 컴포넌트**
  - 로고 + 제품명 "QuakeGuard"
  - 현재 날짜/시각 표시
  - 상태 표시 (대기중 / 분석중 / 완료)

- [x]  **InputPanel 컴포넌트**
  - 시/도 선택 드롭다운 (17개 시도)
  - 시/군/구 선택 드롭다운 (시도 선택 시 동적 변경)
  - 규모(M) 슬라이더 (1.0 ~ 9.0, 0.1 단위)
  - 발생 시각 입력 (날짜+시간 picker)
  - 깊이(km) 입력 (선택사항)
  - 시나리오 빠른 선택 버튼 2개
    -  2016 경주 M5.8 자동 입력
    -  2017 포항 M5.4 자동 입력
  - [AI 분석 시작] 버튼 (강조 디자인)

- [x]  **BuildingList 컴포넌트**
  - 선택 지역 기반 건물 필터링
  - 건물 카드: 이름, 준공연도, 내진설계여부, 위험등급 배지
  - 위험도 높은 순 정렬
  - 건물 없을 경우 빈 상태 표시

- [x]  **MapView 컴포넌트** (Leaflet.js)
  - 대한민국 지도 기본 표시
  - 진앙 위치 마커 (빨간 원형 + 애니메이션)
  - 반경 1km/3km/5km 원 표시
  - 취약 건물 위치 마커 (주소 → 좌표 변환: 주요 지역 하드코딩)
  - 마커 클릭 시 건물 정보 팝업

---

##  Day 3 — 7/4 (금) : AI 핵심 기능

### 3-A. 환경변수 설정

- [x]  **`.env` 파일에 API 키 입력**
  - AI가 생성한 `.env` 파일 열기
  - `VITE_API_KEY=여기에_발급받은_키_붙여넣기` 형식으로 저장

### 3-B. LLM 연동 (코어 로직 TDD 적용)

- [x] 🤖 **LLM API 클라이언트 설정** (`hooks/useLLM.ts`)
  - 환경변수에서 키 읽어오기
  - 스트리밍 API 통신 (Async Generator)
  - 로딩/에러 상태 관리
  - 🧪 *TDD: 에러 처리, 스트리밍 상태 변화 테스트 작성*

- [x] 🤖 **시스템 프롬프트 설계**
  - 공무원 톤앤매너 지정
  - 필수 포함 항목 지정 (피해 예상, 즉시 조치, 1시간 조치, 상황보고서)
  - 환각(Hallucination) 방지 지시어 추가 ("주어진 건물 목록 내에서만 언급")

- [x] 🤖 **사용자 프롬프트 조립 함수** (`utils/promptBuilder.ts`)
  - 입력값(규모, 지역 등) 텍스트화
  - 필터링된 취약 건물 리스트 포맷팅 (이름, 준공연도 등)
  - 🧪 *TDD: 데이터 유무에 따른 프롬프트 생성 로직 단위 테스트*

- [x] 🤖 **AiResult 컴포넌트** (`components/AiResult.tsx`)
  - 마크다운 파싱 및 렌더링
  - 타이핑 효과 (스트리밍 UI)

- [x] 🤖 **AI 결과 품질 테스트 및 고도화 (경진대회 심사기준 반영)**
  - 경주 M5.8, 포항 M5.4 시나리오 테스트
  - 주최측 '프롬프트 엔지니어링 예시'에 명시된 6단계 출력 포맷 적용 완료 (심사 가산점 요인)
  - 지진 옥외 대피장소 및 최근 10년 지진 이력 111건(실데이터) 연동 및 지도 시각화 완료

- [x]  **AI 결과 내용 수동 검토**
  - 경주/포항 시나리오 결과 6단계 포맷 정상 출력 확인
  - 동적 데이터(취약 건물 TOP 3, 대피소) 매핑 확인

---

##  Day 4 — 7/5 (토) : 보고서 + 마무리

### 4-A. 보고서 기능

- [x]  **ReportPanel 컴포넌트** (AiResult에 통합 완성)
  - AI 결과를 행정 보고서 형식으로 재구성 (6단계 포맷 적용 완료)
  - 헤더: 재난 상황 요약표 (발생시각, 규모, 위치, 대응단계)
  - 본문: 조치사항 번호 목록
  - 푸터: 작성일시 자동 기입

- [x]  **인쇄 기능**
  - [보고서 인쇄] 버튼 → 브라우저 인쇄 다이얼로그 호출
  - 인쇄용 CSS (`@media print`) 설정 (불필요한 UI 숨김)

- [x]  **텍스트 내보내기**
  - [텍스트 복사] 버튼 → 전체 보고서 클립보드 복사

### 4-B. 품질 완성

- [x]  **전체 디자인 polish**
  - 색상 일관성 점검 완료
  - 카드 그림자, 테두리, 간격 통일 완료
  - 호버 효과, 버튼 피드백 애니메이션 완료
  - 로딩 스켈레톤 UI 완료 (shimmer 애니메이션)

- [x]  **실제 Claude API 연동 준비**
  - ClaudeProvider.ts 완성 (Anthropic SDK 스트리밍)
  - API 키 입력 모달 (실제 AI / 데모 모드 선택)
  - 우측 상단 ⚙️ 버튼으로 언제든 재설정 가능

- [x]  **에러 처리 전체 점검**
  - API 키 없을 때 모달로 안내 (자동 표시)
  - 네트워크 오류 시 🔄 재시도 버튼 (useLLM retry 기능)
  - AI 오류 상태 UI (에러 내용 + 해결 방법 안내)
  - 데이터 없는 지역 처리 (경고 메시지 표시)

- [x]  **반응형 레이아웃 점검**
  - 1920px 와이드 화면 (기본)
  - 1280px 노트북 화면 (media query 적용)
  - 1024px 이하 태블릿 (단일 열 레이아웃 전환)

- [ ]  **수동 UI 테스트** (자동화 없음 — `.agent/rules/tdd.md` 정책)
  > UI 컴포넌트는 자동화 테스트를 작성하지 않는다. 아래 체크리스트를 사람이 직접 브라우저에서 확인한다.
  - [ ] 시나리오 버튼 "경주 M5.8" 클릭 → 입력값이 자동으로 채워지는지 확인
  - [ ] 시나리오 버튼 "포항 M5.4" 클릭 → 입력값이 자동으로 채워지는지 확인
  - [ ] [AI 분석 시작] 버튼 클릭 → 로딩 스피너가 표시되는지 확인
  - [ ] AI 결과 텍스트가 스트리밍으로 타이핑되듯 실시간 출력되는지 확인
  - [ ] 즉시조치 / 1시간내 조치 / 보고서 섹션이 시각적으로 구분되는지 확인
  - [ ] [복사] 버튼 클릭 → 클립보드에 내용이 복사되는지 확인 (메모장에 붙여넣기로 검증)
  - [ ] [보고서 인쇄] 버튼 클릭 → 브라우저 인쇄 창이 열리는지 확인
  - [ ] 지역 변경 시 건물 목록이 해당 지역으로 필터링되는지 확인
  - [ ] API 키가 없는 상태에서 분석 시작 시 오류 안내 메시지가 뜨는지 확인
  - [ ] 수정 요청사항 정리해서 AI에게 전달

---

##  Day 5 — 7/6 (일) : 배포 + 공모계획서

### 5-A. 배포 준비

- [ ]  **GitHub 계정 생성** (없는 경우)
  - https://github.com → Sign up

- [ ]  **Vercel 계정 생성**
  - https://vercel.com → Continue with GitHub

- [ ]  **Git 초기화 및 코드 커밋**
  - `.gitignore` 파일 설정 (`.env` 파일 제외 — API 키 보호)
  - 전체 코드 커밋

- [ ]  **GitHub 저장소 생성 및 push**
  - 저장소명: `quakeguard`
  - Public/Private 설정 (공모전이므로 Private 권장)

- [ ]  **Vercel 배포 설정**
  - GitHub 저장소 연결
  - 환경변수(API 키) Vercel에 입력
  - Build 설정 확인 (`npm run build`)

- [ ]  **배포된 URL 접속 확인**
  - 모바일에서도 접속해보기
  - 시나리오 버튼 최종 테스트

### 5-B. 공모계획서 작성

- [ ]  **공모계획서 텍스트 초안 작성** (붙임2 양식 기준)
  - 계획서 요약 (공익성·수익성·데이터활용·기술적성과)
  - 공모전 개요 (배경, 목적, 서비스 설명)
  - 창업 아이디어 공익성·수익성
  - 데이터 활용 계획
  - 사업화 가능성

- [ ]  **한글(HWP) 프로그램에 내용 옮겨 붙이기**
  - AI가 작성한 텍스트를 HWP 열어서 붙여넣기
  - 붙임1(참가신청서), 붙임3(동의서), 붙임4(개인정보) 양식 출력

---

##  Day 6 — 7/7 (월) : 최종 제출

- [ ]  **공모계획서 최종 검토 및 완성** (10페이지 이내)
- [ ]  **참가신청서 작성** (붙임1 양식)
- [ ]  **참가동의서 서명** (붙임3 양식)
- [ ]  **개인정보 수집·이용 동의서 서명** (붙임4 양식)
- [ ]  **제출 방법 확인** (마감 전 반드시 확인)
  - 공고문에 온라인/이메일 접수 방법이 명시되지 않아 확인 필요
  - 문의: 행안부 재난안전데이터과 **044-205-4469**
  - 또는 NIA: **heosj@nia.or.kr**
- [ ]  **최종 제출 완료**

---

##  의존성 순서 (이걸 어기면 막힘)

```
Node.js 설치
    ↓
프로젝트 생성 ()
    ↓
safetydata 데이터 확보 () ──→ CSV 파싱 ()
                                        ↓
API 키 발급 () ──────────────→ LLM 연동 ()
                                        ↓
                               UI + AI 통합 ()
                                        ↓
                          GitHub/Vercel 계정 ()
                                        ↓
                                  배포 ()
```

---

##  지금 당장 시작 (오늘 Day 1)

1.  https://nodejs.org → LTS 설치
2.  https://www.safetydata.go.kr → 회원가입 + 데이터 다운로드 가능 여부 확인
3.  Claude 또는 OpenAI API 키 발급
4.  위 3가지 완료 → AI에게 알려주기 → 즉시 코딩 시작

---

*작성일: 2026-07-02 v2 | QuakeGuard*
